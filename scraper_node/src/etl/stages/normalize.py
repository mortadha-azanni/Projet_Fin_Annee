"""Normalization stage stub."""

from ..contracts import PipelineContext


def stage_normalize(context: PipelineContext) -> PipelineContext:
    """Normalize and clean raw fields."""
    return context
